# {{ project_name }}

{{ project_description }}

{%- if use_github %}

[![CI]({{ github_repo_url }}/actions/workflows/ci.yml/badge.svg)]({{ github_repo_url }}/actions/workflows/ci.yml)
[![Coverage](https://img.shields.io/badge/coverage-{{ min_coverage }}%25-green.svg)]()
[![Python Version](https://img.shields.io/badge/python-{{ min_python_version }}+-blue.svg)](https://www.python.org/downloads/)
{%- if license %}
[![License: {{ license }}](https://img.shields.io/badge/License-{{ license }}-yellow.svg)]()
{%- endif %}
{%- endif %}

## Getting Started

### Prerequisites

- Python {{ min_python_version }} or higher
- [uv](https://github.com/astral-sh/uv) package manager (recommended)

### Installation

```bash
# Install uv if you haven't already
curl -LsSf https://astral.sh/uv/install.sh | sh

# Clone the repository
{%- if use_github %}
git clone {{ github_repo_url }}
{%- else %}
git clone <your-repository-url>
{%- endif %}
cd {{ project_slug }}

# Install dependencies and set up the environment
uv sync
```

### Quick Start

{%- if include_cli %}

Run the application:

```bash
uv run {{ package_name }} --help
```
{%- else %}

Use the package in your Python code:

```python
from {{ package_name }} import main

result = main()
print(result)
```
{%- endif %}

## Development

### Running Tests

```bash
# Run all tests
{%- if include_quality_scripts %}
uv run quality-test
{%- else %}
uv run pytest
{%- endif %}

# Run with coverage report
{%- if include_quality_scripts %}
uv run quality-test --coverage
{%- else %}
uv run pytest --cov=src --cov-report=term-missing
{%- endif %}
```

### Code Quality

{%- if include_quality_scripts %}

This project uses centralized quality scripts for consistency:

```bash
# Run all quality checks (format + lint + test)
make check
# or
uv run quality-check

# Format code
uv run quality-format

# Lint code
uv run quality-lint

# Security checks
uv run quality-security
```
{%- else %}

```bash
# Format code
uv run black src tests
uv run isort src tests

# Lint code
uv run ruff check src tests
uv run mypy src tests
```
{%- endif %}

{%- if include_pre_commit %}

### Pre-commit Hooks

Install pre-commit hooks to automatically check code quality before commits:

```bash
uv run pre-commit install
```
{%- endif %}

### Available Commands

The project includes a Makefile for common tasks:

```bash
make help        # Show all available commands
make check       # Run all quality checks
make test        # Run tests
make coverage    # Run tests with coverage report
make format      # Format code
make lint        # Run linters
make clean       # Clean build artifacts
```

## Project Structure

```
{{ project_slug }}/
├── src/
│   └── {{ package_name }}/
│       ├── __init__.py
│       └── main.py           # Main application code
├── tests/
│   ├── __init__.py
│   ├── conftest.py          # Pytest configuration
│   └── test_main.py         # Test files
{%- if include_quality_scripts %}
├── scripts/
│   └── quality/             # Quality check scripts
{%- endif %}
{%- if include_ai_tools %}
├── AI_DOCS/                 # AI development guidelines
│   ├── ai-tools.md
│   ├── code-conventions.md
│   ├── project-context.md
│   └── tdd-workflow.md
{%- endif %}
{%- if include_docs %}
├── docs/                    # Documentation
├── mkdocs.yml               # Documentation configuration
{%- endif %}
{%- if include_docker %}
├── Dockerfile
├── docker-compose.yml
{%- endif %}
{%- if include_pre_commit %}
├── .pre-commit-config.yaml
{%- endif %}
├── pyproject.toml           # Project configuration
├── Makefile
└── README.md
```

{%- if include_ai_tools %}

## AI-Assisted Development

This project includes tools for AI coding assistants to maintain context across sessions.

### Quick Reference

```bash
# Start working on a task
uv run ai-start-task "Add new feature"

# Log progress
uv run ai-log "Implemented feature X"

# Update task plan
uv run ai-update-plan "Write tests"

# View context
uv run ai-context-summary

# Finish task
uv run ai-finish-task --summary="Added feature X with tests"
```

### Supported AI Tools

{%- if ai_tools_preset == "all" %}
- **Claude Code** - See `CLAUDE.md`
- **Cursor IDE** - See `.cursorrules`
- **GitHub Copilot** - See `.github/copilot-instructions.md`
- **Google Gemini** - See `.gemini/styleguide.md`
- **Aider** - See `.aider.conf.yml`
- **Universal** - See `AGENTS.md`
{%- elif ai_tools_preset == "claude" %}
- **Claude Code** - See `CLAUDE.md`
- **Universal** - See `AGENTS.md`
{%- elif ai_tools_preset == "cursor" %}
- **Cursor IDE** - See `.cursorrules`
- **Universal** - See `AGENTS.md`
{%- else %}
- **Universal** - See `AGENTS.md`
{%- endif %}

For detailed AI development guidelines, see the `AI_DOCS/` directory.

{%- endif %}

{%- if include_docker %}

## Docker

Build and run with Docker:

```bash
# Build image
docker build -t {{ project_slug }} .

# Run container
docker run {{ project_slug }}

# Or use docker-compose
docker-compose up
```
{%- endif %}

{%- if include_docs %}

## Documentation

View documentation locally:

```bash
# Serve documentation
mkdocs serve

# Build documentation
mkdocs build
```
{%- endif %}

## Contributing

Contributions are welcome! Please follow these guidelines:

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/my-feature`
3. Write tests first (TDD)
4. Make your changes
5. Run quality checks: `make check`
6. Commit your changes: `git commit -m 'Add my feature'`
7. Push to the branch: `git push origin feature/my-feature`
8. Open a Pull Request

### Development Workflow

This project follows **Test-Driven Development (TDD)**:

1. ✅ Write tests first
2. ✅ Run tests (they should fail)
3. ✅ Implement the feature
4. ✅ Run tests (they should pass)
5. ✅ Run `make check` to ensure quality
6. ✅ Commit your changes

## License

{%- if license %}
This project is licensed under the {{ license }} License. See [LICENSE](LICENSE) for details.
{%- else %}
See [LICENSE](LICENSE) for license information.
{%- endif %}

## Author

**{{ author_name }}**
{%- if author_email %}
- Email: {{ author_email }}
{%- endif %}
{%- if use_github and github_username %}
- GitHub: [@{{ github_username }}](https://github.com/{{ github_username }})
{%- endif %}

## Acknowledgments

Generated from [python-modern-template](https://github.com/Atyantik/python-modern-template) - A modern Python project template with best practices and comprehensive tooling.
